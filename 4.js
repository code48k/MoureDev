// 4.- Imprime por consola el valor de todas las variables.

var global = 12;
console.log(global);

let variableMutable = "Variable Mutable";
console.log(variableMutable);

const variableNoMutable = "Variable no Mutable";
console.log(variableNoMutable);